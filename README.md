# PowerShell Extensions

> Before build, you must add the `.env` file with `TARGET` variable, which should contain the path to the target directory where the built files will be placed.

To build, run `build.ps1` in the project directory. 